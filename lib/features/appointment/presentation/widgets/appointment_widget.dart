import 'package:flutter/material.dart';

class AppointmentWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Appointment Widget'),
    );
  }
}
