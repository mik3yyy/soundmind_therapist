import 'package:flutter/material.dart';

class PatientWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Patient Widget'),
    );
  }
}
