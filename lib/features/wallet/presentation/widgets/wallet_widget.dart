import 'package:flutter/material.dart';

class WalletWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Wallet Widget'),
    );
  }
}
